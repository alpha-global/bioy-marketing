<footer class="">
  <p class="p-4 mt-12 font-medium text-lg text-center md:text-xl md:text-left lg:container lg:mx-auto">
    {{ site.title }}
  </p>

  <div class="bg-stone text-center md:text-left mb-16 md:mb-0">

    <div class="lg:container p-4 mx-auto grid gap-4 md:grid-cols-2">
      <div>
        {{ footer[locale].headline }}

        <div class="mt-4 mb-4">
          {{ footer[locale].cta }}

          <div class="max-w-sm grid gap-4 grid-cols-2 md:flex md:gap-0 mx-auto md:mx-0">
            <a href="{{ globals.stores.ios }}" class="block" target="_blank">
              {% svgSprite name="app-store-ios", class="h-16 w-full md:w-32 md:mr-4" %}
              <span class="visually-hidden">Download on the App Store</span>
            </a>
            <a href="{{ globals.stores.android }}" class="block" target="_blank">
              {% svgSprite name="app-store-android", class="h-16 w-full md:w-32" %}
              <span class="visually-hidden">Get it on Google Play</span>
            </a>
          </div>
        </div>

        <div class="flex items-center justify-center md:justify-start">
          {% if footer[locale].social.instagram %}
          <a href="https://instagram.com/{{ footer.social.instagram }}" rel="noopener" target="_blank" class="flex items-center justify-center rounded-full bg-red text-white fill-current mx-1 h-10 w-10 hover:bg-red-dark md:ml-0 md:mr-4">
            {% svgSprite name="instagram", class="h-6 w-6 fill-current" %}
            <span class="visually-hidden">Instagram</span>
          </a>
          {% endif %}

          {% if footer[locale].social.facebook %}
          <a href="https://facebook.com{{ footer.social.instagram }}" rel="noopener" target="_blank" class="flex items-center justify-center rounded-full bg-red text-white fill-current mx-1 h-10 w-10 hover:bg-red-dark md:ml-0 md:mr-4">
            {% svgSprite name="facebook", class="h-6 w-6 fill-current" %}
            <span class="visually-hidden">Facebook</span>
          </a>
          {% endif %}

          {% if footer[locale].social.twitter %}
          <a href="https://twitter.com{{ footer.social.instagram }}" rel="noopener" target="_blank" class="flex items-center justify-center rounded-full bg-red text-white fill-current mx-1 h-10 w-10 hover:bg-red-dark md:ml-0 md:mr-4">
            {% svgSprite name="twitter", class="h-6 w-6 fill-current" %}
            <span class="visually-hidden">Twitter</span>
          </a>
          {% endif %}
        </div>

      </div>

      <ul class="md:grid md:gap-4 md:grid-cols-2 md:w-2/3 md:justify-self-end md:mt-8">
        {% for link in footer[locale].nav %}
          <li class="inline pr-4">
            <a href="{{ link.href }}" class="underline text-red hover:text-red-darker hover:no-underline">
              {{ link.label }}</a>
          </li>
        {% endfor %}
      </ul>

      <div class="text-xs text-grey">
        <p class="my-2">BiOY is part of Alpha International. Alpha International is a charity registered in England & Wales (no. 1086179) and in Scotland (no. SC042906) and a private company limited by guarantee and registered in England & Wales (no. 4157379). The registered office is at HTB Brompton Road SW7&nbsp;1JA.</p>
        <p class="my-2">&copy Copyright Alpha International {{ globals.currentYear }}</p>
      </div>
  </div>
</footer>
