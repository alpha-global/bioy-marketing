---
layout: default
locale: en
---

Pick language:
<div class="flex">
  {% for lang in site.languages %}
  <div class="mr-4">
    <a href="/{{ lang.locale }}">{{ lang.language }}</a>
  </div>
  {% endfor %}
</div>

{% set locale = 'en' %}
